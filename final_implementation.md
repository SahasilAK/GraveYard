 ## STEP 25 — Automated Validation Gate Against Placeholder Output
Add an automated check between Developer output and QA's smoke check (fromStep 6) that catches placeholder/mock/pseudocode output before it's everconsidered "done," as a safety net on top of the prompt fix in Step 24 —prompts alone are not 100% reliable.1. Implement a heuristic validator that flags likely placeholder code, checking for patterns such as: - Function bodies that are only a print/log statement and nothing else. - Comments containing "TODO", "placeholder", "pseudo", "not implemented", "mock" in place of real logic. - Functions whose docstring/task description implies real work (e.g. "parse CSV", "connect to database") but whose body doesn't reference the expected library/operation at all. - Suspiciously short implementations relative to what the AtomicTask description implies is needed.2. When the validator flags an issue, do not let it pass through to QA's smoke check. Instead, feed the specific flagged reason back to the Developer agent as corrective context and trigger a regeneration attempt for that AtomicTask (bounded retry count, consistent with the error- handling middleware from what is now Step 19 — surface to the human if it still fails after max retries).3. Log every validation flag and retry outcome clearly in the Logs section, so a human can see when and why the system caught and corrected a placeholder-output attempt.4. This validator is a heuristic safety net, not a substitute for QA's real smoke check — keep it fast and cheap (no full model call required for the basic pattern checks; a model-based "does this look like real implementation?" check can be added as an optional secondary pass if the heuristics prove insufficient in testing).Test: intentionally have the Developer produce a stub (or temporarily revertthe Step 24 prompt fix) and confirm the validator catches it, triggers acorrective retry, and logs the event — then confirm normal realimplementations pass through without false-positive flags.
```
 ## STEP 26 — Rewrite All Agent Base Prompts with Expert Persona & Strict Format
Rewrite the system prompt for every agent (Prompt Agent, PO, Scrum Master,Developer, QA, Reviewer) using a consistent, strong structure: expertpersona framing, explicit scope/responsibilities, explicit output format,and explicit constraints — replacing any current generic/thin prompts.Use this structure for each agent's prompt:1. Persona line — a specific, credible expert framing, e.g.: - PO: "You are an expert Product Owner with deep experience writing clear, testable user stories and unambiguous acceptance criteria for software teams." - Scrum Master: "You are an expert Scrum Master and technical orchestrator, skilled at sequencing work, tracking blockers, and enforcing sprint discipline." - Developer: "You are a senior software engineer with 15+ years of production experience across multiple languages, known for writing complete, correct, well-structured, real working code — never placeholders or partial implementations." - QA: "You are an expert QA engineer specializing in both fast smoke testing and thorough functional test design." - Reviewer: "You are a principal-level tech lead responsible for code review, standards enforcement, and merge gatekeeping." - Prompt Agent: "You are an expert requirements analyst who converts unstructured user input into precise, structured, unambiguous project briefs."2. Scope/responsibilities — a short explicit list of exactly what this agent does and does not do (reuse the Section 2 role definitions from the original build prompt as the source of truth).3. Output format — the exact structured format expected (e.g. the Plan/ Task/AtomicTask or DiffTask models from what is now Step 15/17), stated explicitly rather than left implicit, including a minimal example of correctly-formatted output.4. Hard constraints — explicit "never do X" rules relevant to that role (Developer: never produce placeholders/mocks/pseudocode, per Step 24; PO: never skip acceptance criteria; QA: never approve without running the smoke check; Reviewer: never approve code it hasn't actually inspected; Prompt Agent: never invent requirements the user didn't state).5. Keep every prompt token-efficient despite the added structure — concise, direct sentences, no filler, consistent with the system's overall token-efficiency goal. A stronger prompt should not mean a bloated one.Store these as the canonical prompt templates (e.g. in scrum_team/agents/prompts/ as separate files per agent) so they're easy to audit and tuneindependently, rather than inlined as long strings scattered through thecode.Test: re-run the full end-to-end sample flow from Step 9/Step 21 andconfirm output quality improves across every role, not just the Developer —check PO acceptance criteria specificity, QA test-case quality, andReviewer feedback depth before/after the prompt rewrite.

 ## STEP 27 — Prompt Regression Testing
Add regression coverage so future prompt edits can't silently reintroduceplaceholder output or vague/low-quality output from any agent, extendingthe test harness from what is now Step 21.1. Create a small fixed set of sample AtomicTasks/Tasks covering a few different kinds of work (e.g. a file-parsing task, an API-endpoint task, a bugfix task) and store expected quality characteristics for each (e.g. "must contain real csv.reader usage," "must not contain the string 'placeholder' or a print-only function body," "must define at least N lines of real logic").2. Add automated tests that run these fixture tasks through the Develope. agent and assert against the Step 25 validator (must pass, zero flags. plus the specific quality characteristics defined above.3. Add similar lightweight assertions for PO output (acceptance criteria present and non-empty, specific rather than generic), QA output (test cases reference the actual feature, not generic boilerplate), and Reviewer output (references specific code, not a generic "looks good").4. Run this test set any time an agent's base prompt (from Step 26) is modified, before trusting the change against real projects — document this as the required check in the README/CONTRIBUTING notes for future prompt tuning.This closes the loop: Step 23 diagnosed the problem, Step 24 fixed theDeveloper's behavior, Step 25 added a runtime safety net, Step 26 raisedoutput quality across every agent, and Step 27 protects all of it againstregressions going forward.

## step 28 the project should include full things like the tests readme , documenentations example usage userguide etc remeber the team is building a fully productuion grade prodcuts
